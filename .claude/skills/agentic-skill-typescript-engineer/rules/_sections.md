7 categories ranked by impact:

1. **CRITICAL**: Core Principles (`core-principles`) - foundational type safety: end-to-end typesafety, narrow types, maximum inference
2. **CRITICAL**: Typing (`typing`) - type definitions, interface vs type, readonly, zod, inference, return types
3. **HIGH**: Error Handling (`error-handling`) - Result/Option monads, no throwing in domain logic
4. **MEDIUM**: Switch Exhaustiveness (`switch-exhaustiveness`) - union exhaustiveness via satisfies never
5. **MEDIUM**: Imports (`imports`) - ordering, grouping, type imports separation
6. **MEDIUM**: Code Style (`code-style`) - naming, spacing, guard clauses, nesting depth
7. **LOW**: Strategy Pattern (`strategy-pattern`) - when and how to apply strategy pattern
